{
    "defaultConnection": "default",
    "connections": {
      "default": {
        "connector": "bookshelf",
        "settings": {
          "client": "postgres",
          "host": "${process.env.DATABASE_HOST || 'd4c.ckayt2ufowsn.eu-central-1.rds.amazonaws.com'}",
          "port": "${process.env.DATABASE_PORT || 5432}",
          "database": "${process.env.DATABASE_NAME || 'postgres'}",
          "username": "${process.env.DATABASE_USERNAME || 'dropsforcrops'}",
          "password": "${process.env.DATABASE_PASSWORD || 'dropsforcrops'}"
        },
        "options": {
          "ssl": false
        }
      }
    }
  }